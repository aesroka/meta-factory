# Core dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0   # load .env into os.environ for API key fallbacks
litellm>=1.40.0        # Phase 2: unified LLM layer, cost tracking, tiers

# LLM Providers (install what you need)
anthropic>=0.18.0          # Claude (default)
openai>=1.0.0              # GPT-4, also used for Deepseek
google-generativeai>=0.3.0 # Gemini

# CLI
click>=8.0.0
rich>=13.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# RAG (Forge-Stream Phase 1)
# Note: ragflow-sdk requires Python >=3.12; use HTTP client below if on 3.9
# ragflow-sdk>=0.23.0
requests>=2.28.0   # RAGFlow HTTP API client

# Future phases (uncomment when needed)
# pypdf>=3.0.0     # PDF parsing for Bibles
